{
  "buildCommand": "npm run -w @stock-tracker/validation build && npm run -w @stock-tracker/types build && npx expo export --platform web",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
