import { create } from "zustand";
import type { TrackerHistoryState } from "./tracker-history.type";

export const useTrackerHistoryStore = create<TrackerHistoryState>()(() => ({
  // TODO: Define initial state
}));
