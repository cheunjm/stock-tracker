import { create } from "zustand";
import type { TrackerDashboardState } from "./tracker-dashboard.type";

export const useTrackerDashboardStore = create<TrackerDashboardState>()(() => ({
  // TODO: Define initial state
}));
